import React from 'react';

const FullBin = props => {

    return (
        <div className="bin">
            <h2>Bin {props.bin}</h2>
            {/* {props.product}
            {props.price} */}
        </div>
    )

}

export default FullBin;
